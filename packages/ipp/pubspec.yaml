name: ipp
description: >
  Transport-agnostic codec and minimal client/server transport for the IPP
  (Internet Printing Protocol) binary encoding defined in RFC 8010/8011.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
